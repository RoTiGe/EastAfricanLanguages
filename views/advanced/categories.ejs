<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container-fluid p-0">
        <header class="text-white py-4 position-relative">
            <div class="container">
                <a href="/" class="btn btn-light position-absolute top-0 start-0 m-3">
                    <i class="bi bi-arrow-left me-2"></i>Back to Home
                </a>
                <h1 class="text-center display-5 fw-bold"><%= title %></h1>
            </div>
        </header>

        <main class="container my-4">
            <div class="alert alert-info">
                <i class="bi bi-info-circle me-2"></i>
                Browse hierarchical category structure with priority levels and persona mappings
            </div>

            <div class="row">
                <% categories.forEach((category, index) => { %>
                <div class="col-12 col-md-6 col-lg-4 mb-4">
                    <div class="card h-100 shadow-sm">
                        <div class="card-header bg-primary text-white">
                            <h3 class="h5 mb-0">
                                <span class="badge bg-light text-primary me-2">P<%= category.priority %></span>
                                <%= category.name %>
                            </h3>
                        </div>
                        <div class="card-body">
                            <p class="text-muted small"><%= category.description %></p>
                            
                            <h4 class="h6 mb-2">Subcategories:</h4>
                            <div class="mb-3">
                                <% category.subcategories.forEach(sub => { %>
                                <span class="badge bg-secondary me-1 mb-1"><%= sub %></span>
                                <% }) %>
                            </div>
                            
                            <h4 class="h6 mb-2">Personas:</h4>
                            <div>
                                <% category.personas.forEach(persona => { %>
                                <span class="badge bg-info me-1 mb-1"><%= persona %></span>
                                <% }) %>
                            </div>
                        </div>
                    </div>
                </div>
                <% }) %>
            </div>
        </main>

        <footer class="bg-dark text-white text-center py-4 mt-5">
            <div class="container">
                <p class="mb-0">Category Network Browser - Advanced Mode</p>
            </div>
        </footer>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
