<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Participate - Language Bridge</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">
                <i class="bi bi-soundwave me-2"></i>Language Bridge
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link active" href="/participate">
                            <i class="bi bi-upload me-1"></i>Participate
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/donate">
                            <i class="bi bi-heart-fill me-1"></i>Donate
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/about">
                            <i class="bi bi-info-circle me-1"></i>About Us
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container my-5">
        <!-- Header -->
        <div class="text-center mb-5">
            <h1 class="display-4 fw-bold mb-3">
                <i class="bi bi-people-fill text-primary me-3"></i>Participate
            </h1>
            <p class="lead text-muted">
                Help us expand our language database! Share your translations and contribute to breaking down language barriers.
            </p>
        </div>

        <!-- Info Alert -->
        <div class="alert alert-info shadow-sm mb-4">
            <h4 class="alert-heading">
                <i class="bi bi-info-circle-fill me-2"></i>How to Contribute
            </h4>
            <p>We welcome translation contributions in any format:</p>
            <ul class="mb-0">
                <li><strong>JSON files:</strong> Follow our format (download template below)</li>
                <li><strong>Text files:</strong> List phrases with translations</li>
                <li><strong>Spreadsheets:</strong> CSV or Excel files with phrase pairs</li>
                <li><strong>Documents:</strong> Any format with clear source and target languages</li>
            </ul>
        </div>

        <!-- Template Download -->
        <div class="card shadow-sm mb-4">
            <div class="card-body">
                <h5 class="card-title">
                    <i class="bi bi-file-earmark-arrow-down me-2"></i>Download Template
                </h5>
                <p class="card-text text-muted">
                    Download our translation template to see the expected format
                </p>
                <a href="/api/translation-template" class="btn btn-outline-primary">
                    <i class="bi bi-download me-2"></i>Download JSON Template
                </a>
            </div>
        </div>

        <!-- Submission Form -->
        <div class="card shadow-sm">
            <div class="card-body p-4">
                <h3 class="h4 mb-4 text-primary">
                    <i class="bi bi-upload me-2"></i>Submit Translation
                </h3>

                <form id="participateForm" enctype="multipart/form-data">
                    <!-- Language Selection -->
                    <div class="row g-3 mb-4">
                        <div class="col-12 col-md-6">
                            <label for="sourceLanguage" class="form-label fw-semibold">
                                <i class="bi bi-translate me-2"></i>Source Language
                            </label>
                            <select id="sourceLanguage" name="sourceLanguage" class="form-select form-select-lg" required>
                                <option value="">-- Select source language --</option>
                                <% languages.forEach(language => { %>
                                <option value="<%= language %>"><%= languageNames[language] || language %></option>
                                <% }) %>
                                <option value="other">Other (specify in comments)</option>
                            </select>
                        </div>
                        <div class="col-12 col-md-6">
                            <label for="targetLanguage" class="form-label fw-semibold">
                                <i class="bi bi-flag me-2"></i>Target Language
                            </label>
                            <select id="targetLanguage" name="targetLanguage" class="form-select form-select-lg" required>
                                <option value="">-- Select target language --</option>
                                <% languages.forEach(language => { %>
                                <option value="<%= language %>"><%= languageNames[language] || language %></option>
                                <% }) %>
                                <option value="other">Other (specify in comments)</option>
                            </select>
                        </div>
                    </div>

                    <!-- File Upload -->
                    <div class="mb-4">
                        <label for="translationFile" class="form-label fw-semibold">
                            <i class="bi bi-file-earmark-text me-2"></i>Upload Translation File
                        </label>
                        <input type="file" class="form-control form-control-lg" id="translationFile" name="translationFile" 
                               accept=".json,.txt,.csv,.xlsx,.xls,.doc,.docx" required>
                        <div class="form-text">
                            Accepted formats: JSON, TXT, CSV, Excel, Word
                        </div>
                    </div>

                    <!-- Comments -->
                    <div class="mb-4">
                        <label for="comments" class="form-label fw-semibold">
                            <i class="bi bi-chat-left-text me-2"></i>Comments / Additional Information
                        </label>
                        <textarea class="form-control" id="comments" name="comments" rows="4" 
                                  placeholder="Add any context, notes, or specify languages if you selected 'Other'..."></textarea>
                    </div>

                    <!-- Contact Information -->
                    <div class="row g-3 mb-4">
                        <div class="col-12 col-md-6">
                            <label for="contactName" class="form-label fw-semibold">
                                <i class="bi bi-person me-2"></i>Your Name
                            </label>
                            <input type="text" class="form-control" id="contactName" name="contactName" 
                                   placeholder="Enter your name" required>
                        </div>
                        <div class="col-12 col-md-6">
                            <label for="contactEmail" class="form-label fw-semibold">
                                <i class="bi bi-envelope me-2"></i>Email Address
                            </label>
                            <input type="email" class="form-control" id="contactEmail" name="contactEmail" 
                                   placeholder="your.email@example.com" required>
                        </div>
                    </div>

                    <!-- Organization (Optional) -->
                    <div class="mb-4">
                        <label for="organization" class="form-label fw-semibold">
                            <i class="bi bi-building me-2"></i>Organization (Optional)
                        </label>
                        <input type="text" class="form-control" id="organization" name="organization" 
                               placeholder="Your organization or institution (if applicable)">
                    </div>

                    <!-- Terms Checkbox -->
                    <div class="form-check mb-4">
                        <input class="form-check-input" type="checkbox" id="termsAgree" required>
                        <label class="form-check-label" for="termsAgree">
                            I agree to share this translation freely under a Creative Commons license and understand 
                            it will be publicly available.
                        </label>
                    </div>

                    <!-- Submit Button -->
                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-primary btn-lg">
                            <i class="bi bi-send-fill me-2"></i>Submit Translation
                        </button>
                    </div>

                    <!-- Status Message -->
                    <div id="statusMessage" class="mt-3" style="display: none;"></div>
                </form>
            </div>
        </div>

        <!-- Thank You Section -->
        <div class="card shadow-sm mt-4 border-success">
            <div class="card-body text-center py-4">
                <h5 class="card-title text-success">
                    <i class="bi bi-heart-fill me-2"></i>Thank You for Contributing!
                </h5>
                <p class="card-text text-muted mb-0">
                    Every translation helps bridge communication gaps and brings communities closer together.
                </p>
            </div>
        </div>
    </div>

    <footer class="bg-dark text-white text-center py-4 mt-5">
        <div class="container">
            <p class="mb-0">
                <i class="bi bi-globe me-2"></i>Language Bridge - Tour world languages one at a time
            </p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.getElementById('participateForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            
            const formData = new FormData(e.target);
            const statusDiv = document.getElementById('statusMessage');
            
            statusDiv.style.display = 'block';
            statusDiv.className = 'alert alert-info';
            statusDiv.textContent = 'Uploading...';
            
            try {
                const response = await fetch('/api/submit-translation', {
                    method: 'POST',
                    body: formData
                });
                
                const data = await response.json();
                
                if (response.ok) {
                    statusDiv.className = 'alert alert-success';
                    statusDiv.innerHTML = '<i class="bi bi-check-circle-fill me-2"></i>' + data.message;
                    e.target.reset();
                } else {
                    throw new Error(data.error || 'Submission failed');
                }
            } catch (error) {
                statusDiv.className = 'alert alert-danger';
                statusDiv.innerHTML = '<i class="bi bi-exclamation-triangle-fill me-2"></i>Error: ' + error.message;
            }
        });
    </script>
</body>
</html>
